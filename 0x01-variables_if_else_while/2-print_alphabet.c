#include <stdio.h>
/**
 * main - Entry point to print the letters in lower case.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char 2_alphabet = a;
	while (2_alphabet <= 'z')
	{
		putchar ("%c", 2_alphabet);
		2_alphabet++;
	}
	return (0);
}
