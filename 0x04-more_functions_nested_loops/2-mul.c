#include "main.h"
/**
 * mul - multiplies two integers
 * @a: int type number
 * @b: int type number.
 * Return: returns the product of the two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
