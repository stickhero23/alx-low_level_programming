#include "main.h"
/**
 * add - finds the sume of two integers x and y.
 *@x: int type number
 *@y: int type number
 * Return: Returns the sum of two integers.
 */
int add(int x, int y)
{
	return (x + y);
}
